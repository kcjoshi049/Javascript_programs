let age = 25;
let new_age = "25";
console.log(age == new_age);
console.log(age === new_age);
console.log(age != new_age);
console.log(age !== new_age);


/* 
    "==" :- is is used to check the value is same or not but not to check the type of the data;
    "===" :- it is used to check the value as well as the type of the data it returns true if data value and type both are same and false if type or value does'nt matches.

*/
